import DashboardShell from "@/components/DashboardShell";

const weights: Array<[string, number]> = [["Compétences obligatoires",30],["Similarité sémantique",20],["Compétences souhaitées",10],["Compatibilité domaine",15],["Expérience",10],["Formation",5],["Localisation",5],["Disponibilité",5]];
export default function MatchingSettings(){return <DashboardShell role="admin" title="Paramètres matching" subtitle="Les poids sont configurables : aucun entraînement sur dataset n'est nécessaire."><div className="mx-auto max-w-4xl card"><div className="rounded-xl bg-cmc-sky p-5 text-sm leading-6 text-gray-600"><strong className="text-cmc-navy">Moteur hybride explicable :</strong> comparaison des compétences, embeddings / similarité sémantique et règles métier. Les poids ci-dessous seront ensuite chargés depuis la base de données.</div><div className="mt-6 space-y-4">{weights.map(([name,value])=><div key={String(name)} className="grid items-center gap-3 rounded-xl border border-gray-100 p-4 md:grid-cols-[1fr_120px]"><div><p className="font-bold text-cmc-navy">{name}</p><div className="mt-2 h-2 rounded-full bg-gray-100"><div className="h-2 rounded-full bg-cmc-teal" style={{width:`${value*3}%`}}/></div></div><div><input type="number" className="input text-center font-bold" defaultValue={value}/><p className="mt-1 text-center text-xs text-gray-400">poids %</p></div></div>)}</div><div className="mt-6 flex items-center justify-between rounded-xl bg-gray-50 p-4"><span className="font-extrabold text-cmc-navy">Total</span><span className="text-2xl font-extrabold text-cmc-teal-dark">100%</span></div><button className="btn-primary mt-6 w-full">Enregistrer la configuration</button></div></DashboardShell>}
