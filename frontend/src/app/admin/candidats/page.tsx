"use client";
import Link from "next/link";
import { Search } from "lucide-react";
import { useMemo, useState } from "react";
import DashboardShell from "@/components/DashboardShell";
import { candidates } from "@/data/mock";

export default function AdminCandidates(){const [type,setType]=useState("ALL"); const [q,setQ]=useState(""); const list=useMemo(()=>candidates.filter(c=>(type==="ALL"||c.type===type)&&c.name.toLowerCase().includes(q.toLowerCase())),[type,q]); return <DashboardShell role="admin" title="Stagiaires & lauréats" subtitle="Une seule base candidat, avec séparation fonctionnelle et analytique par type."><div className="card p-0 overflow-hidden"><div className="flex flex-wrap items-center justify-between gap-4 border-b border-gray-100 p-5"><div className="flex gap-2">{[["ALL","Tous"],["STAGIAIRE","Stagiaires"],["LAUREAT","Lauréats"]].map(([v,l])=><button key={v} onClick={()=>setType(v)} className={type===v?"btn-primary":"btn-secondary"}>{l}</button>)}</div><div className="relative w-full md:w-72"><Search size={17} className="absolute left-3 top-3 text-gray-400"/><input value={q} onChange={e=>setQ(e.target.value)} className="input pl-10" placeholder="Rechercher un candidat"/></div></div><div className="overflow-x-auto"><table className="w-full text-left text-sm"><thead className="bg-gray-50 text-xs uppercase tracking-wider text-gray-500"><tr><th className="px-5 py-4">Candidat</th><th className="px-5 py-4">Type</th><th className="px-5 py-4">Filière</th><th className="px-5 py-4">Candidatures</th><th className="px-5 py-4">Activité</th><th></th></tr></thead><tbody>{list.map(c=><tr key={c.id} className="border-t border-gray-100"><td className="px-5 py-4"><p className="font-bold text-cmc-navy">{c.name}</p><p className="text-xs text-gray-400">{c.email}</p></td><td className="px-5 py-4"><span className={`badge ${c.type==="STAGIAIRE"?"bg-cmc-sky text-cmc-teal-dark":"bg-gray-100 text-cmc-navy"}`}>{c.type}</span></td><td className="px-5 py-4 text-gray-600">{c.filiere}</td><td className="px-5 py-4 font-bold text-cmc-navy">{c.applications}</td><td className="px-5 py-4 text-gray-600">{c.activity}</td><td className="px-5 py-4 text-right"><Link href={`/admin/candidats/${c.id}`} className="font-bold text-cmc-teal-dark">Voir le profil →</Link></td></tr>)}</tbody></table></div></div></DashboardShell>}
